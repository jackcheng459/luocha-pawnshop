# 罗刹当铺开发交接说明

## 项目概况

《罗刹当铺》是 React + Vite + Tailwind CSS 实现的 H5 志怪小游戏。核心流程为：开场入店、来意选择、摇命签、命格卡、入门典当、货架交易、可选续签、当票卡、离店。

线上地址：https://luocha-pawnshop.vercel.app

## 技术栈

- React + Vite
- TypeScript
- Tailwind CSS
- 状态管理：`useReducer`
- LLM：Vercel Function 代理 DeepSeek API
- 分享卡导出：`html2canvas`
- 动效：`framer-motion` + CSS animation
- 音频：外部 BGM + Web Audio API 程序化音效

## 本地启动

```bash
npm install
npm run dev
```

如果要在本地完整测试 `/api/llm`，建议使用：

```bash
npx vercel@latest dev
```

## 环境变量

不要把真实 key 写入前端或提交到仓库。线上 Vercel Production 已配置：

```text
DEEPSEEK_API_KEY
DEEPSEEK_MODEL=deepseek-v4-flash
```

本地可参考 `.env.example`。

## 关键目录

- `src/App.tsx`：主流程与阶段切换
- `src/game/reducer.ts`：游戏状态机
- `src/game/rules.ts`：交易、价格、资源规则
- `src/game/fate.ts`：命签 fallback 生成
- `src/data/fateStories.ts`：命签故事与分享钩子
- `src/data/items.ts`：货架商品
- `src/services/llmClient.ts`：前端 LLM 调用与 fallback
- `api/llm.ts`：Vercel Function LLM proxy
- `src/services/audioEngine.ts`：BGM 与程序化音效
- `src/components/TransitionVeil.tsx`：墨幕转场
- `public/images/`：游戏主视觉资产
- `public/audio/`：音频资产与授权说明
- `docs/`：接入、视觉、音频、参考记录

## 常用命令

```bash
npm run lint
npm run build
npx vercel@latest deploy --yes --prod
```

## 注意事项

1. 游戏内金额单位显示为“两/钱”，内部计算使用“钱”。
2. DeepSeek 通过 `api/llm.ts` 代理，不能在前端暴露 key。
3. LLM 调用失败时必须走 fallback，不能让主流程中断。
4. 当前开源版本 BGM 使用 `public/audio/bgm/a-really-dark-alley.mp3`，许可为 CC0。
5. `public/audio/bgm/luocha.mp3` 为本地项目提供素材，已从开源仓库排除，确认授权后可自行恢复。
6. `Night-Patrol` 只作为交互设计参考，不能复制其代码、音频、视频或视觉资产用于本项目。
