# 《罗刹当铺》视觉与体验全面升级 · Codex 执行文档

> 当前状态：网站已上线（https://luocha-pawnshop.vercel.app），主流程跑通，但视觉与体验"不够高级"。
> 本文档目标：把作品从"功能 demo"升级为"有叙事推进的志怪小游戏"。
> 你（Codex）的角色：自主规划视觉资产体系 + 实现叙事过渡 + 集成代码。

---

## 一、整体目标

### 1.1 体验目标

把 5 个关键瞬间从"瞬间跳转"改成"叙事推进"：

```
打开网站 → 雾散见铺（开场）
点击进入 → 推门入店（过渡）
开始游戏 → 摇签求命（核心仪式）
每次交易 → 掌柜现身（对话过渡）
购买商品 → 物品获得（小高潮）
游戏结束 → 当票拼合（收尾）
```

整局体验目标：**90-150 秒**（含所有过渡动画）。

### 1.2 视觉目标

**整体基调**：
- 风格：聊斋绣像本插画 + 清代版画 + 水墨写意 + 丰子恺笔触
- 倾向：半文半白（介于纯写实国画和现代插画之间）
- 配色：深褐暗调为主、朱砂红点缀、烛光暖色提亮
- 暗度：中暗（适合手机屏阅读，但保持志怪沉郁感）

**精确配色**：

| 用途 | 色值 |
|------|------|
| 主背景（深褐墨色） | `#3a2818` |
| 次背景（暖棕） | `#6b4423` |
| 纸面色（泛黄宣纸） | `#d4b896` |
| 强调色（朱砂红） | `#8b1a1a` |
| 烛光暖色 | `#d4a574` |
| 文字主色（墨色） | `#3a2818` |
| 文字浅色（赭石） | `#a67c52` |

**绝对禁区**：
- 彩色霓虹、塑料质感、扁平卡通、赛博朋克
- 紫渐变、蓝色冷调、纯黑、纯白
- 现代游戏 UI 模板（蓝条、彩按钮、卡通字体）
- 免费图标库的通用图（购物车、信封、文件等）
- emoji（破坏志怪氛围）

---

## 二、字体系统（必须先做，性价比最高）

在 `index.html` 的 `<head>` 加：

```html
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Ma+Shan+Zheng&family=ZCOOL+XiaoWei&family=Noto+Serif+SC:wght@400;500;700&display=swap" rel="stylesheet">
```

CSS 类名约定：

```css
/* 全局默认 */
body {
  font-family: 'Noto Serif SC', '思源宋体', serif;
}

/* 毛笔字标题：罗刹当铺、命格名、商品名、关键标签 */
.title-brush, .fate-name, .item-name {
  font-family: 'Ma Shan Zheng', cursive;
  letter-spacing: 0.05em;
}

/* 古风正文：掌柜台词、判词、商品描述、隐藏文案 */
.literary-text, .shopkeeper-line, .judgment, .item-description {
  font-family: 'ZCOOL XiaoWei', serif;
  letter-spacing: 0.04em;
}
```

---

## 三、视觉资产体系

### 3.1 你的工作流程

按以下顺序工作。**每一步完成后让我看，不要一口气做完。**

**第一步**：你先列一份完整资产清单。包括：
- 资产名 / 用途 / 尺寸 / 是否透明 / 后处理需求 / 用什么技术（AI 生图 / SVG / CSS）/ 你打算用的 prompt / 优先级
- 这份清单存到 `docs/visual-assets.md`，方便后期回查

**第二步**：等我审完清单。审的重点：
- AI 生图 vs SVG 的分工是否合理
- 优先级是否对（4:3 比赛展示图必须在最高优先级）
- 风格关键词是否准确

**第三步**：开始执行。每张 AI 生图：
- 跑 6-10 个候选
- 你自己挑最符合美术目标的 1 张
- 必要时做后处理（抠图 / 裁切 / 调色 / 加渐变）
- 放到 `public/images/` 或 `public/textures/`
- 保留 prompt 和参数到 `docs/visual-assets.md`

**第四步**：集成到组件，配合 Framer Motion 实现叙事过渡。

### 3.2 AI 生图 vs SVG 的分工原则

**适合 AI 生图（位图）**：
- 写实场景（远景当铺、店内主场景）
- 需要真实笔触质感（剪影、皮影感）
- 复杂氛围（雾、灯光、纸张做旧）
- 4:3 比赛展示图

**适合 SVG（矢量代码）**：
- 几何道具（签筒、签、灯笼）
- 朱印（需要可调字体内容）
- 装饰元素（边框、纹饰、铜钱）
- 需要交互/动画的元素（摇晃、闪烁）

**适合 CSS（纯样式）**：
- 雾气漂移效果
- 油灯光晕呼吸
- 纸张纹理（用 noise pattern）
- 通用过渡动画

如果你判断某个资产应该走另一条路（比如签筒用 AI 生图比 SVG 好），告诉我理由，我可以接受调整。

### 3.3 关键资产参考清单（你可以增删）

以下是我的初步建议，**你可以根据自己的判断调整**：

| 资产 | 用途 | 技术 | 优先级 |
|------|------|------|--------|
| 4:3 比赛展示图 | 比赛提交物 | AI 生图 | **P0 最高** |
| 开场远景当铺 | 雾散见铺那一幕 | AI 生图 | P0 |
| 店内主场景 | 进店后主背景 | AI 生图 | P0 |
| 掌柜剪影（半身） | 对话时升起 | AI 生图 + 抠图 | P0 |
| 签筒 | 摇签那一幕 | SVG | P1 |
| 签（一支） | 摇签后弹出 | SVG | P1 |
| 命格朱印 | 命格卡命格名 | SVG + CSS | P1 |
| 灯笼 | 当铺装饰、可复用 | SVG | P1 |
| 当票纸纹 | 当票卡背景 | CSS noise / SVG | P1 |
| 雾气层 | 开场 + 全局氛围 | CSS | P1 |
| 油灯光晕 | 店内场景增强 | CSS | P2 |
| 朱印（"已得""收"等） | 物品获得动画 | SVG | P2 |

### 3.4 关键 AI 生图 prompt 参考

以下是参考 prompt，**你可以基于此优化**。重点是保持风格一致。

#### 4:3 比赛展示图（最重要！）

```
聊斋志异风格的志怪当铺夜景，
木桌上点着一盏油灯，桌上放着签筒和泛黄当票，
桌后是模糊的老掌柜半身剪影，戴方巾蓄须，
墙上挂着字画卷轴，窗外飘着朦胧灯笼，
雾气缭绕在脚边，深棕暗调，烛光暖色点缀，
清代版画质感，丰子恺笔触感，
左侧或右侧留出空白处可加"罗刹当铺"标题文字，
4:3 横版构图，氛围沉郁神秘

negative: 现代元素、卡通圆头、扁平塑料、彩色霓虹、
蓝色、紫色、写实摄影、人物面部清晰、3D 渲染、
赛博朋克、科幻、二次元
```

#### 开场远景当铺

```
夜色中一座古代当铺的远景剪影，
建筑深褐色暗调，屋檐微翘，
门口挂着两只朦胧红灯笼（发暖光），
门头匾额隐约可见"當"字（不必清晰），
两扇朱红木门半掩，门内透出昏黄灯光，
四周笼罩着浅淡夜雾，雾气从地面升起，
聊斋绣像本插画风格，清代版画质感，
留白处可融入夜色，便于雾气过渡，
16:9 比例，整体偏暗

negative: 现代元素、写实摄影、3D、赛博朋克、彩色霓虹、白天
```

#### 店内主场景

```
昏暗的志怪当铺内景，
木质柜台一角占据画面下半部分，
柜台上一盏摇曳的油灯，散发暖黄色光晕，
柜台上散落几张当票、一只签筒、一只算盘，
柜台后挂着字画卷轴和字幅（字迹不必清晰），
墙角隐约有竹签筒和木架，
雾气在柜台周围淡淡浮动，
深棕暗调底色，烛光暖色重点照明，
聊斋插图风格，丰子恺笔触感，
画面中央和上方留出空白便于摆放交易 UI，
4:3 比例，氛围沉郁

negative: 现代元素、塑料感、扁平、卡通、彩色、明亮、人物
```

#### 掌柜半身剪影（必须背景透明）

```
一位古代当铺老掌柜的半身侧影剪影，
深褐色或纯黑剪影，背景完全透明，
戴方巾，长须飘垂，神态沉静难辨，
身穿长袍，姿态略前倾仿佛在打量客人，
半身构图（从腰部以上至头顶），
聊斋插图风格的人物剪影，
类似皮影戏的简洁有力，
没有面部细节，只有轮廓
PNG 格式，透明背景

negative: 面部表情、彩色、写实、3D、现代服饰、卡通
```

**特别提醒**：剪影类必须输出 PNG 透明背景。如果生成模型不支持透明背景，**先生成纯绿幕底（#00ff00），然后用 rembg 或类似工具抠图**。这一步是 op7418 工作流的精髓——你应该早就规划好。

---

## 四、五幕戏过渡实现

安装依赖：
```
npm install framer-motion react-countup
```

### 第一幕：开场雾散见铺（页面加载后 3-4 秒）

**视觉路径**：
```
全黑 (0.5s)
↓
雾气从底部 + 两侧飘入，覆盖全屏 (0.5s)
↓
当铺远景图淡入显现，雾气逐渐变淡 (1.5s)
↓
"罗刹当铺"四字毛笔字从下淡入 (0.6s)
↓
副标题"夜深了，进来坐坐"出现 (0.4s)
↓
"推门入店"按钮浮现 (0.3s)
```

**关键实现**：
- 雾气用 CSS 三层错开的径向渐变 + animation
- 当铺图用 Framer Motion 控制 opacity + scale
- "罗刹当铺"用 Ma Shan Zheng 字体 + 朱砂红 + 大尺寸

### 第二幕：入店过渡（点击按钮后 1.5 秒）

**视觉路径**：
```
当铺剪影放大 + 边缘渐渐模糊（推进感） (0.8s)
↓
切换到店内场景（fade）(0.5s)
↓
桌上出现一句话："今夜你来得巧。先摇一卦。"（打字机）(1s)
```

**关键实现**：
- 推进感用 Framer Motion 的 scale + filter blur
- 场景切换用 AnimatePresence

### 第三幕：摇签求命（核心仪式感，4-5 秒）

**这是整个体验的灵魂瞬间，不能省，不能简化。**

**视觉路径**：
```
店内场景，桌上中央一只签筒（SVG 绘制）
↓
玩家点击签筒
↓
签筒摇晃 1.5 秒（rotate keyframes + 抖动）
↓
一支签从筒中跳出（spring 弹出动画）
↓
签悬浮在空中，玩家可点击
↓
玩家点击签 → 签展开（旋转 + 放大消失）
↓
命盘逐项浮现（5 项命数 stagger 0.2s）
↓
命格名朱印从屏幕上方"砰"地盖落（scale 4→1 + rotate）
↓
判词淡入
↓
按钮出现："入店典当" 或 "再摇一次（折慧一两）"
```

**关键实现**：

```jsx
// 签筒摇晃
animate={{
  rotate: [-3, 3, -2, 2, -1, 1, 0],
  x: [-2, 2, -1, 1, 0, -1, 0]
}}
transition={{ duration: 1.5 }}

// 签弹出
initial={{ y: 100, opacity: 0, rotate: -10 }}
animate={{ y: 0, opacity: 1, rotate: 0 }}
transition={{ type: 'spring', stiffness: 100 }}

// 朱印盖落（关键瞬间！）
initial={{ y: -200, scale: 4, rotate: -20, opacity: 0 }}
animate={{ y: 0, scale: 1, rotate: -2, opacity: 1 }}
transition={{ delay: 1.5, duration: 0.4, type: 'spring', stiffness: 200 }}
```

### 第四幕：掌柜出场（每次需要他说话时）

**视觉路径**：
```
屏幕下方升起掌柜半身剪影（200px 高，从 y:200 → y:0）(0.6s)
↓
旁边浮现台词框（左侧或上方）
↓
台词打字机出现（每字 60-80ms）
↓
末尾出现"点击继续"提示
↓
玩家点击屏幕任意处
↓
剪影下沉，台词淡出 (0.4s)
↓
进入下一步操作
```

**触发场景**：
- 摇签前
- 入门典当时（"此物随心，价由老朽定"）
- 玩家想买但资源不足时（"典无可典，何以为买？"）
- 抽签结果揭晓时
- 离店前（临别赠言）

**关键实现**：
- 剪影组件 ShopkeeperDialog，接受 text prop 和 onContinue 回调
- 打字机用 useEffect + setInterval 实现

### 第五幕：物品获得（每次购买商品后 2-3 秒）

**视觉路径**：
```
点击"取" 按钮
↓
商品卡片飘起（scale 1.2 + y 上移）(0.4s)
↓
朱印从空中盖落（"已得"或"收"）(0.3s)
↓
数值变化动画（react-countup 滚动）(0.6s)
↓
副作用浮现（小卡片，半透明朱砂红边框）(0.4s)
↓
全部淡出 (0.5s)
↓
回到货架
```

**关键实现**：
- 用 AnimatePresence 包裹整个 overlay
- CountUp 用于数值变化
- 副作用浮现用 stagger

### 离场：当票拼合

**视觉路径**：
```
玩家点击"结当离店"
↓
画面渐暗
↓
当票纸张从中央展开（scale 0 → 1）
↓
内容逐项填入（命格名、交易记录、命数变化、临别赠言）
↓
"罗刹当铺"朱印盖落
↓
日期戳显现
↓
"明日还有明日的事"淡入小字（在当票下方）
↓
分享按钮出现
```

---

## 五、跳过机制

**所有过渡动画都必须有跳过机制**。玩家点击屏幕任意处可立即跳到该幕结尾。

实现：每个动画 overlay 顶层加 `onClick` 监听，触发 `skipToEnd`。

跳过机制能避免两个风险：
- 评委不耐烦，2 分钟体验时长被动画拖累
- 重复体验时玩家想直奔主题

---

## 六、加载状态与 fallback

**所有 LLM 调用必须有 fallback**（这一条之前已经强调，这里再强化）：

```typescript
async function callLLM(prompt: string, fallback: string): Promise<string> {
  try {
    const result = await Promise.race([
      fetch('/api/llm', { method: 'POST', body: JSON.stringify({ prompt }) }),
      new Promise((_, reject) => setTimeout(() => reject('timeout'), 5000))
    ]);
    if (!result.ok) throw new Error();
    return await result.text();
  } catch {
    return fallback;
  }
}
```

LLM 失败时，前端不应有任何报错提示给用户。直接走 fallback 文本，玩家无感知。

**资产加载 fallback**：

如果某张图加载失败：
- 远景图失败 → 用 CSS 渐变 + SVG 当铺替代
- 店内场景失败 → 用 CSS 纯色 + 木质纹理
- 掌柜剪影失败 → 用 SVG 简笔人物

`<img onError>` 监听必须接入。

---

## 七、性能与移动端

- 所有 AI 生图压缩到 < 200KB（用 squoosh 或类似工具）
- 单页 JS 控制在 < 300KB
- 首屏 LCP 目标 ≤ 3 秒（移动端）
- 所有动画使用 transform 和 opacity，避免触发 layout
- 移动端最大宽度 600px，PC 端居中显示，两侧留暗色背景

---

## 八、时间盒与红线

### 时间盒（建议）

| 任务 | 预计 | 性质 |
|------|------|------|
| 字体系统 | 10 分钟 | P0 必做 |
| 资产清单审核 | 15 分钟 | P0 必做 |
| 4:3 比赛展示图 | 30 分钟（含迭代） | P0 必做 |
| 开场远景图 + 雾气 | 30 分钟 | P0 必做 |
| 店内主场景图 | 30 分钟 | P0 必做 |
| 摇签那一幕（含签筒签 SVG） | 60 分钟 | P0 必做 |
| 掌柜剪影 + 打字机 | 40 分钟 | P0 必做 |
| 物品获得动画 | 30 分钟 | P1 |
| 当票卡精修 | 30 分钟 | P1 |
| 朱印效果 | 20 分钟 | P1 |
| 货架视觉分级 | 20 分钟 | P2 |

### 红线

如果时间不够，按以下顺序砍：
1. 先砍 P2（货架视觉分级）
2. 再砍 P1 中的物品获得动画（用简化版替代）
3. 最后砍 P1 中的当票卡精修（用文字版）

**绝对不能砍**：
- 4:3 比赛展示图（这是评委进 demo 之前看到的唯一图像）
- 摇签那一幕（核心爽点）
- 字体系统（性价比最高）

---

## 九、最终交付物清单

完成后告诉我以下都做好了：

- [ ] 字体系统接入
- [ ] 4:3 比赛展示图（独立设计的横版主视觉，不是分享卡裁切）
- [ ] 开场雾散见铺（含远景图）
- [ ] 入店过渡
- [ ] 摇签求命（含签筒、签的 SVG，朱印盖落动画）
- [ ] 掌柜剪影 + 打字机对话
- [ ] 物品获得动画（朱印 + 数值滚动）
- [ ] 当票卡精修（朱印、纸张纹理、布局）
- [ ] 货架视觉分级（按档位差异化）
- [ ] 所有动画的跳过机制
- [ ] 所有 LLM 调用的 fallback
- [ ] 所有图片的 onError fallback
- [ ] 移动端响应式
- [ ] `docs/visual-assets.md` 完整记录

---

## 十、立刻开始：第一步动作

**你现在做的第一件事**：

读完本文档后，列出一份资产清单（按 3.1 节要求），存到 `docs/visual-assets.md`。

清单字段：
```markdown
| # | 资产名 | 用途 | 技术 | 尺寸 | 透明 | 后处理 | Prompt | 优先级 |
|---|--------|------|------|------|------|--------|--------|--------|
| 1 | luocha-pawnshop-hero.jpg | 4:3 比赛展示图 | AI 生图 | 1600x1200 | 否 | 标题文字叠加 | (完整 prompt) | P0 |
| 2 | shop-facade.png | 开场远景当铺 | AI 生图 | 1920x1080 | 否 | 底部渐变到透明 | (prompt) | P0 |
| ... |
```

列完之后告诉我，我审完再开工。

不要直接生图。不要直接改代码。先列清单。

---

**文档版本**：v1.0
**创建日期**：2026 年 5 月 1 日
**适用 Codex 版本**：具备图像生成能力的版本（OpenAI Codex with GPT-Image / 类似集成）
**项目 URL**：https://luocha-pawnshop.vercel.app
