---
title: 《罗刹当铺》v1.5 增量设计文档
version: v1.5
type: 增量开发指令 / Codex 交付版
date: 2026-05
author: 程建都
status: 开发就绪
prev_version: v1.0（已上线 https://luocha-pawnshop.vercel.app）
target_platform: Vercel + 跨平台兼容（预留微信小程序迁移路径）
---

# 《罗刹当铺》v1.5 增量设计文档

> 这份文档专门给 Codex 用。
> 阅读对象：实施开发的人或 AI。
> 目标：在 v1 基础上增加三件新事——命主故事、临别金句库、命格卡二维码故事入口。
> **本文档所有功能都遵守一条铁则**：不引入注册账号、不引入用户体系、不引入登录数据库。

---

## 0. 总览

### 0.1 v1.5 要做的三件事

| 模块 | 功能 | 优先级 |
|------|------|--------|
| 命主故事 | 每局完成后，LLM 生成一段当夜叙事，永久存储 | P0 |
| 临别金句库 | LLM 批量生成临别词，持久化到后端，人工审核迭代 | P0 |
| 二维码 + 故事入口 | 命格卡上嵌入二维码，扫码访问命主故事页 | P0 |

### 0.2 v1.5 不做的事

- 不做注册账号
- 不做登录系统
- 不做用户数据库
- 不做赎回机制（留 v2）
- 不做物物转让（留 v2）
- 不做 UGC 公共货架（留 v2）

### 0.3 跨平台迁移就绪性

**这是核心约束**。v1.5 的所有数据存储和 API 设计**必须满足**：

- 数据层与业务层解耦
- 不绑定 Vercel 特定服务（如 Vercel KV）的高级特性
- API 路由命名通用化，不依赖平台 routing 约定
- 静态资源（图片、字体）路径独立可迁移
- 数据导出/导入工具齐备

将来如果转其他平台或微信小程序，**只需替换数据层和身份层，业务层零改动**。

---

## 1. 命主故事

### 1.1 触发时机

玩家完成一局后，**当票卡生成同时**，异步调用 LLM 生成命主故事。

```
玩家点击"结当离店"
    ↓
当票卡前端立即渲染（不等故事）
    ↓
后端异步：
  - 生成当票 ID（UUID）
  - 调用 LLM 生成命主故事
  - 存储到后端
  - 返回故事 ID
    ↓
前端命格卡上嵌入二维码（指向故事 ID）
```

### 1.2 LLM 调用规格

#### Input Schema

```typescript
interface FateStoryInput {
  // 命格信息
  fateName: string;              // 如 "慧剑斩痴"
  fateJudgment: string;          // 判词
  
  // 命数变化
  initialResources: ResourceMap;  // 开局五项命数
  finalResources: ResourceMap;    // 离店五项命数
  
  // 交易记录
  trades: Array<{
    type: 'pawn' | 'buy';
    resourceFrom?: ResourceKey;
    resourceTo?: ResourceKey;
    amountFrom?: number;
    amountTo?: number;
    itemName?: string;            // 玩家命名（典当时）或商品名（购买时）
    itemStory?: string;           // 玩家来历（仅典当时）
  }>;
  
  // 抽签结果
  drewLot: boolean;
  lotResult?: 'shang' | 'zhong' | 'xia';
  
  // 时间戳（古典纪年格式）
  timestamp: string;              // 如 "丙午年某月某夜子时三刻"
}

type ResourceKey = "chi" | "chen" | "tan" | "wang" | "hui";
type ResourceMap = Record<ResourceKey, number>; // 单位：钱
```

#### Output Schema

```typescript
interface FateStoryOutput {
  storyId: string;               // UUID，故事唯一 ID
  storyText: string;             // 300-500 字命主故事正文
  generatedAt: string;           // ISO 时间戳
  llmModel: string;              // 使用的 LLM 模型名（用于追溯）
  promptVersion: string;         // 使用的 prompt 版本号
}
```

#### Prompt 设计

```
你是罗刹当铺的记账先生，受老掌柜之托，将今夜进店者的故事写成笔记一则。

## 文风要求（必须严格遵守）

文风锚点：纪昀《阅微草堂笔记》+ 袁枚《子不语》

- 第二人称叙事（讲"客"或"君"的故事，但允许偶尔用"你"以增加代入感）
- 长度严格 300-500 字
- 句子要短，一句话讲一件事
- 不展开心理描写（"客面有惘色"，不要"客内心如刀绞"）
- 白描胜于修辞
- 结尾必须开放，不能给"圆满结局"或"励志收束"
- 必须呼应玩家选择中最特殊的那一个（典当物、买的物件、抽签结果之一）
- 古今词汇混用要克制，可以用"乙未夜"，但不要用"霓虹""量子"等现代词
- 微微讥诮可以有，但不要用力

## 体例参考

正确示范：
"丙午年某月某夜子时三刻，客自远而来。客面有惘色，怀珠一颗，欲解烦恼。掌柜问其名，曰'半生迷雾珠'。掌柜笑而收之，付以三枚迷惘钱。客观货架良久，未取一物。复向柜上典出旧念两钱，又典出一两痴。离店之时，掌柜执笔写票，曰：'客官走得稳，因为疼过。'客出门，雾已散。"

错误示范（太鸡汤）：
"你那年没说出口的话，是因为你害怕被拒绝。其实她也在等你说。多年之后回头看，你才明白真正的勇气不是没有恐惧，而是在恐惧中依然前行。"

错误示范（用力过猛装古风）：
"汝当年欲言又止，盖心怀畏惧也。然伊人亦默默相期。岁月如梭，回首方知，真勇者非无惧也，乃惧而前行也。诚哉斯言！"

## 输入信息

时间：{timestamp}
命格：{fateName}
判词：{fateJudgment}
初始命数：{initialResources}
离店命数：{finalResources}
本夜交易：
{trades}
抽签：{lotResult}

## 输出要求

仅输出故事正文，不要包含任何标题、说明、或括号备注。

直接以"{timestamp}，..." 开头。
```

### 1.3 持久化存储

#### 数据模型

```typescript
interface FateStoryRecord {
  storyId: string;                // primary key, UUID v4
  fateName: string;
  fateJudgment: string;
  initialResources: ResourceMap;
  finalResources: ResourceMap;
  trades: TradeRecord[];
  drewLot: boolean;
  lotResult?: string;
  timestamp: string;              // 古典纪年时间戳
  storyText: string;              // LLM 生成的故事正文
  generatedAt: number;            // Unix 时间戳，便于查询/排序
  llmModel: string;
  promptVersion: string;
  
  // 隐私字段（不对外展示）
  userFingerprint?: string;       // 浏览器指纹哈希，仅用于反爬虫
  ipHash?: string;                // IP 哈希，仅用于审计
}
```

**重要**：`userFingerprint` 和 `ipHash` 仅作内部审计/反滥用，**不能用于识别用户、不能跨故事关联、不能展示给前端**。

#### 存储方案

**v1.5 默认方案**：Vercel KV（Redis 兼容）

```
Key: story:{storyId}
Value: JSON.stringify(FateStoryRecord)
TTL: 永久（不设过期）
```

**抽象层**（必做，方便迁移）：

```typescript
// /lib/storage/StoryStorage.ts
interface StoryStorage {
  save(story: FateStoryRecord): Promise<void>;
  get(storyId: string): Promise<FateStoryRecord | null>;
  list(options?: { limit?: number; before?: number }): Promise<FateStoryRecord[]>;
  delete(storyId: string): Promise<void>;
  exportAll(): Promise<FateStoryRecord[]>;  // 用于迁移
}

// /lib/storage/VercelKVStorage.ts （默认实现）
class VercelKVStorage implements StoryStorage { ... }

// 将来迁移可加：
// /lib/storage/SupabaseStorage.ts
// /lib/storage/WechatCloudStorage.ts
```

业务层只调用 `StoryStorage` 接口，不直接调 Vercel KV API。

#### API 路由

```
POST /api/story/create     创建命主故事（内部接口，由完成一局自动触发）
GET  /api/story/:storyId   获取命主故事（公开接口，二维码扫码访问）
```

**安全考虑**：

- `POST /api/story/create` 不暴露给前端直接调用，只能由后端服务在游戏结束时调用
- `GET /api/story/:storyId` 是公开的，不需要任何身份验证（任何人持有 storyId 都能查看）
- 故事页面上**不显示任何创建者身份信息**（无昵称、无头像、无 ID）

### 1.4 故事页面 (`/story/[storyId]`)

#### 页面结构

```
┌─────────────────────────────────┐
│   罗刹当铺 · 一夜笔记            │
│   丙午年某月某夜子时三刻         │
├─────────────────────────────────┤
│                                 │
│   命格：慧剑斩痴                │
│   判词：你困在往事里...          │
│                                 │
│   ┌─ [命主故事正文 300-500字] ─┐│
│   │                            ││
│   │   ...                       ││
│   │                            ││
│   └────────────────────────────┘│
│                                 │
│   [当票卡缩略图]                 │
│                                 │
│   ┌────────────────────────┐   │
│   │  我也走一遭            │   │
│   └────────────────────────┘   │
│                                 │
└─────────────────────────────────┘
```

#### 关键交互

- 故事页面**纯只读**，不允许评论、点赞、分享统计
- "我也走一遭"按钮 → 跳转到游戏首页（`/`），开始新一局
- 不显示创建者信息
- 页面可被搜索引擎索引（promote 故事流量）

#### 视觉风格

完全继承游戏内调性：泛黄宣纸纹理、Ma Shan Zheng 标题、ZCOOL XiaoWei 正文、深褐底色、朱砂红强调。

---

## 2. 临别金句库

### 2.1 设计目标

替代当前固定的几句临别赠言（"放下是福，客官慢走"等），建立一个**持续生长的金句库**，让每位玩家拿到的临别词都能"懂他"。

### 2.2 金句调子分类

按种子样本提炼出五大调子：

| 类别 | 调子特征 | 种子示例 | 触发条件 |
|------|---------|---------|---------|
| 认出疼过型 | 承认残缺，不修复 | "客官走得稳，因为疼过。" | 大量典当（≥2 次重当） |
| 少骗自己型 | 微微讥诮但温柔 | "今夜没白来，你少骗自己一次。" | 卖出大量痴或惘 |
| 抬举玩家型 | 不卑不亢的尊重 | "此处已留你不住，人间还未必配得上你。" | 慧值显著上升 |
| 戛然而止型 | 三五字戛然而止 | "灯下莫回头。" | 大量买入痴或嗔类商品 |
| 温柔守护型 | 余温不煽情 | "明日的你，不必谢今夜的你。" | 整体损耗较小，轻轻走过 |

### 2.3 LLM 批量生成

#### Prompt 设计

```
你是罗刹当铺的老掌柜，需要为玩家写一句临别赠言。

## 核心要求

这句话不是评分，不是打气，不是说教。
是一位看过太多事的老者，对一位走过一夜的进店者，说一句"我看到你了"。

## 文风约束

- 不要"加油""你最棒""你做得对"等鸡汤
- 不要"愿你幸福""祝君平安"等套话
- 不要"成长""勇气""力量"等励志词
- 字数 8-20 字，控制在两个短句以内
- 阅微草堂笔记 + 子不语调子，半文白
- 承认残缺，不修复残缺
- 半句没说，留余韵

## 调子样本（按类别）

### 认出疼过型
- 客官走得稳，因为疼过。
- 此物压心多年，今夜终于落柜。

### 少骗自己型
- 今夜没白来，你少骗自己一次。
- 客官话不多，今夜想必想透了一些。

### 抬举玩家型
- 此处已留你不住，人间还未必配得上你。
- 客官走得轻，配得上一夜好梦。

### 戛然而止型
- 灯下莫回头。
- 夜路上慢些。
- 来日再来。

### 温柔守护型
- 明日的你，不必谢今夜的你。
- 天亮前回得来便好。

## 任务

请为"{type}"调子生成 5 句新的临别赠言。

要求：
1. 每句独立成一个项目
2. 严格符合该调子的特征
3. 与样本风格一致但不重复样本
4. 不要编号，不要解释，不要标点之外的额外字符
5. 一句一行
```

#### 批量生成流程

```typescript
async function generatePhraseBatch(
  type: PhraseType,
  count: number = 5
): Promise<Phrase[]> {
  const prompt = buildPrompt(type);
  const response = await llm.complete(prompt);
  const lines = response.split('\n').filter(line => line.trim());
  
  return lines.map(line => ({
    id: uuid(),
    type,
    text: line.trim(),
    status: 'pending_review',  // 初始状态：待审核
    createdAt: Date.now(),
    llmModel: 'claude-3.5-sonnet',
    promptVersion: 'v1.5.0',
  }));
}
```

### 2.4 持久化存储

#### 数据模型

```typescript
interface Phrase {
  id: string;                    // UUID
  type: 'recognize_pain' | 'less_self_deception' | 'elevate' | 'abrupt' | 'gentle_guard';
  text: string;                  // 句子本身
  status: 'pending_review' | 'approved' | 'rejected' | 'archived';
  createdAt: number;
  reviewedAt?: number;
  reviewedBy?: string;           // 审核者标识（不存账号，存"作者"或"团队"）
  llmModel: string;
  promptVersion: string;
  
  // 使用统计（不绑定用户，只是简单计数）
  usedCount: number;             // 被引用次数
  lastUsedAt?: number;
}
```

#### 存储方案

```
Key: phrase:{phraseId}
Value: JSON.stringify(Phrase)

# 索引
Key: phrase:index:by_type:{type}     // SET，存放该类型下所有 approved 状态的 phraseId
Key: phrase:index:pending             // SET，存放所有待审核的 phraseId
```

#### API 路由

```
POST /api/phrase/generate    生成一批新句子（管理员接口）
GET  /api/phrase/list        列出指定状态的句子（管理员接口）
PATCH /api/phrase/:id        更新句子状态（管理员接口）
GET  /api/phrase/random/:type 随机取一句指定类型的 approved 句子（业务接口）
```

#### 管理员审核界面（Admin Dashboard）

新增页面 `/admin/phrases`，简单的审核界面：

```
┌──────────────────────────────────────┐
│  临别金句库管理 · 程建都律师         │
├──────────────────────────────────────┤
│  [生成新批次] 调子: [认出疼过型 ▼]   │
│              数量: [5    ]           │
│                                       │
│  待审核 (12)                          │
│  ┌─────────────────────────────────┐ │
│  │ "客官此夜，疼得有声有色。"       │ │
│  │ [认出疼过型] [✓ 通过] [✗ 拒绝]  │ │
│  └─────────────────────────────────┘ │
│  ┌─────────────────────────────────┐ │
│  │ "今夜未取一物，胜过取走半片屋。" │ │
│  │ [温柔守护型] [✓ 通过] [✗ 拒绝]  │ │
│  └─────────────────────────────────┘ │
│  ...                                  │
│                                       │
│  已通过 (87)                          │
│  - 按类别分组展示                     │
│  - 可导出 JSON                        │
└──────────────────────────────────────┘
```

**身份验证**：用一个**简单的 admin token**（环境变量 `ADMIN_TOKEN`），不需要完整账号系统。访问 `/admin/*` 路由时检查 token。

### 2.5 业务层使用

游戏结束生成当票卡时：

```typescript
async function getFarewellPhrase(playerState: PlayerState): Promise<string> {
  // 根据玩家状态判断类型
  const type = inferPhraseType(playerState);
  
  // 从金句库随机取一句
  const phrase = await phraseStorage.getRandom(type);
  
  if (phrase) {
    // 增加使用计数
    await phraseStorage.incrementUsedCount(phrase.id);
    return phrase.text;
  }
  
  // Fallback：金句库为空时，使用硬编码的兜底句
  return getHardcodedFallback(type);
}

function inferPhraseType(state: PlayerState): PhraseType {
  const heavyPawnCount = state.trades.filter(t => 
    t.type === 'pawn' && t.amountFrom >= 7
  ).length;
  
  const huiGain = state.finalResources.hui - state.initialResources.hui;
  const totalLoss = sumLoss(state.initialResources, state.finalResources);
  
  if (heavyPawnCount >= 2) return 'recognize_pain';
  if (huiGain > 5) return 'elevate';
  if (totalLoss < 5) return 'gentle_guard';
  // ... 其他逻辑
  return 'abrupt';
}
```

### 2.6 跨平台迁移就绪

**所有 phrase 数据可通过一个 API 完整导出**：

```
GET /api/admin/phrases/export
→ 返回完整 JSON 文件，含所有 phrase 记录
```

将来转微信小程序时，导出 JSON，导入新平台数据库即可。

**导出格式示例**：

```json
{
  "version": "v1.5.0",
  "exportedAt": "2026-05-15T10:00:00Z",
  "phrases": [
    {
      "id": "uuid-1",
      "type": "recognize_pain",
      "text": "客官走得稳，因为疼过。",
      "status": "approved",
      "createdAt": 1715760000000,
      "usedCount": 142
    },
    ...
  ]
}
```

---

## 3. 命格卡二维码 + 故事入口

### 3.1 二维码生成

#### 位置

命格卡（PNG 图片）右下角，**12% × 12%** 大小（相对卡片宽度）。

不要太大盖住命格名，不要太小扫不出。

#### 内容

二维码内容是一个完整 URL：

```
https://luocha-pawnshop.vercel.app/story/{storyId}
```

将来如果换域名或转小程序，URL 模板可配置。

#### 生成时机

当票卡生成的同时（命主故事生成完成后），命格卡二维码也一并嵌入。

#### 技术实现

前端用 `qrcode.js` 生成二维码并合成到 PNG 上：

```typescript
import QRCode from 'qrcode';
import html2canvas from 'html2canvas';

async function generateFateCardWithQR(
  fateData: FateData,
  storyId: string
): Promise<Blob> {
  // 1. 渲染命格卡 DOM
  const cardElement = renderFateCardDOM(fateData);
  
  // 2. 生成二维码
  const qrUrl = `https://luocha-pawnshop.vercel.app/story/${storyId}`;
  const qrDataUrl = await QRCode.toDataURL(qrUrl, {
    width: 120,
    margin: 1,
    color: {
      dark: '#3a2818',   // 墨色
      light: '#d4b896',  // 纸面色
    },
  });
  
  // 3. 把二维码插入卡片右下角
  const qrImg = document.createElement('img');
  qrImg.src = qrDataUrl;
  qrImg.style.cssText = `
    position: absolute;
    right: 16px;
    bottom: 16px;
    width: 12%;
    border: 1px solid rgba(58, 40, 24, 0.2);
  `;
  cardElement.appendChild(qrImg);
  
  // 4. 截图为 PNG
  const canvas = await html2canvas(cardElement);
  return new Promise(resolve => canvas.toBlob(resolve, 'image/png'));
}
```

### 3.2 故事页 `/story/[storyId]`

#### Next.js Route

```typescript
// /pages/story/[storyId].tsx 或 /app/story/[storyId]/page.tsx
export async function getServerSideProps({ params }) {
  const { storyId } = params;
  const story = await storyStorage.get(storyId);
  
  if (!story) {
    return { notFound: true };
  }
  
  return { props: { story } };
}

export default function StoryPage({ story }: { story: FateStoryRecord }) {
  return <StoryView story={story} />;
}
```

#### SEO 与社交分享

```typescript
<Head>
  <title>{story.fateName} · 罗刹当铺一夜笔记</title>
  <meta property="og:title" content={`${story.fateName} · 一夜笔记`} />
  <meta property="og:description" content={story.fateJudgment} />
  <meta property="og:image" content="/og-image.png" />
  <meta property="og:type" content="article" />
</Head>
```

#### 页面组件

```jsx
function StoryView({ story }) {
  return (
    <div className="story-page">
      <header className="story-header">
        <div className="brand">罗刹当铺 · 一夜笔记</div>
        <div className="timestamp">{story.timestamp}</div>
      </header>
      
      <main className="story-content">
        <div className="fate-stamp">{story.fateName}</div>
        <div className="judgment">{story.fateJudgment}</div>
        
        <article className="story-text">
          {story.storyText.split('\n').map((p, i) => (
            <p key={i}>{p}</p>
          ))}
        </article>
        
        <div className="receipt-thumbnail">
          {/* 当票卡缩略图，可省略 */}
        </div>
      </main>
      
      <footer className="story-footer">
        <a href="/" className="cta-button">
          我 也 走 一 遭
        </a>
      </footer>
    </div>
  );
}
```

---

## 4. 工程实现要求

### 4.1 文件结构

```
/pages
  /api
    /story
      create.ts       # POST 创建故事
      [storyId].ts    # GET 获取故事
    /phrase
      generate.ts     # POST 批量生成
      list.ts         # GET 列表
      [id].ts         # PATCH 更新
      random.ts       # GET 随机取
    /admin
      /phrases
        export.ts     # GET 导出
  /story
    [storyId].tsx     # 故事页
  /admin
    phrases.tsx       # 金句管理后台

/lib
  /storage
    StoryStorage.ts   # 接口
    VercelKVStorage.ts # 实现
    PhraseStorage.ts  # 接口
    PhraseKVStorage.ts # 实现
  /llm
    storyGenerator.ts # 故事生成
    phraseGenerator.ts # 金句生成
  /utils
    timestamp.ts      # 古典纪年时间工具
    phraseInference.ts # 金句类型推断

/components
  /story
    StoryView.tsx
  /admin
    PhraseAdmin.tsx
```

### 4.2 环境变量

```
# .env.local
ANTHROPIC_API_KEY=...        # 或 DEEPSEEK_API_KEY
KV_URL=...                   # Vercel KV 连接（自动注入）
KV_REST_API_TOKEN=...
ADMIN_TOKEN=...              # 管理员后台访问 token
NEXT_PUBLIC_SITE_URL=https://luocha-pawnshop.vercel.app
```

### 4.3 古典纪年工具函数

```typescript
// /lib/utils/timestamp.ts

const HEAVENLY_STEMS = ['甲', '乙', '丙', '丁', '戊', '己', '庚', '辛', '壬', '癸'];
const EARTHLY_BRANCHES = ['子', '丑', '寅', '卯', '辰', '巳', '午', '未', '申', '酉', '戌', '亥'];
const HOUR_NAMES = ['子', '丑', '寅', '卯', '辰', '巳', '午', '未', '申', '酉', '戌', '亥'];
const QUARTER_NAMES = ['初', '二', '三', '四', '五', '六', '七'];

export function getClassicalTimestamp(date: Date = new Date()): string {
  // 干支年（按公历年简化映射，2026 = 丙午）
  const year = date.getFullYear();
  const stemIdx = (year - 4) % 10;
  const branchIdx = (year - 4) % 12;
  const ganzhiYear = HEAVENLY_STEMS[stemIdx] + EARTHLY_BRANCHES[branchIdx];
  
  // 时辰
  const hour = date.getHours();
  const shichenIdx = Math.floor((hour + 1) / 2) % 12;
  const shichen = HOUR_NAMES[shichenIdx] + '时';
  
  // 刻（随机）
  const quarter = QUARTER_NAMES[Math.floor(Math.random() * 7)] + '刻';
  
  return `${ganzhiYear}年某月某夜${shichen}${quarter}`;
}
```

### 4.4 性能要求

- 命主故事 LLM 调用 ≤ 8 秒，超时走 fallback 简短模板
- 金句库随机取一句 ≤ 100ms（KV 读取应秒级响应）
- 故事页面首屏加载 ≤ 2 秒
- 二维码生成 ≤ 500ms

### 4.5 错误处理

#### LLM 故事生成失败

如果 LLM 失败或超时：
- 仍然创建 storyRecord，但 `storyText` 用模板生成（基于交易记录的简单叙述）
- 标记 `llmModel: 'fallback'`
- 不影响玩家看到当票卡和命格卡

```typescript
function generateFallbackStory(input: FateStoryInput): string {
  return `${input.timestamp}，客自远而来。携${input.trades[0]?.itemName}入店，与掌柜交易数番，离店之时，命数已变。`;
}
```

#### KV 存储失败

- 记录到 Vercel logs
- 故事不持久化，但玩家本局体验不受影响
- 二维码不生成（前端检测 storyId 为 null 时跳过）

---

## 5. 验收清单

开发完成后，以下功能应全部通过验收：

### 5.1 命主故事

- [ ] 玩家完成一局后，后端自动生成命主故事
- [ ] 故事永久存储在 KV
- [ ] 故事可通过 `/story/{storyId}` 访问
- [ ] 故事页面无任何账号信息
- [ ] LLM 生成失败时，fallback 正常工作
- [ ] 故事文风符合阅微+子不语调（人工抽样验证 10 篇）

### 5.2 临别金句库

- [ ] LLM 可批量生成 5-10 句一批
- [ ] 生成的句子默认状态为 `pending_review`
- [ ] 管理后台可审核（通过/拒绝）
- [ ] 通过的句子可被业务层随机取用
- [ ] 金句使用次数有计数
- [ ] 可一键导出全部金句为 JSON

### 5.3 二维码 + 故事入口

- [ ] 命格卡 PNG 右下角嵌入二维码
- [ ] 二维码扫码可正确进入故事页
- [ ] 故事页有"我也走一遭"按钮跳回首页
- [ ] 故事页支持微信内打开（不被屏蔽）
- [ ] 故事页支持分享（OG 标签正确）

### 5.4 跨平台迁移就绪

- [ ] 所有数据存储通过抽象接口（不直接调用 Vercel KV API）
- [ ] 故事和金句都可一键导出 JSON
- [ ] 时间戳、命格、金句逻辑均独立于平台
- [ ] 文档说明了如何替换数据层

### 5.5 立场守护（最重要）

- [ ] **没有引入任何注册账号机制**
- [ ] **没有引入任何登录机制**
- [ ] **没有引入任何用户体系**
- [ ] **没有引入日登录奖励**
- [ ] **没有引入排行榜或成就**
- [ ] **故事属于那一夜，不属于那个用户**

---

## 6. 给 Codex 的工作流程建议

### 6.1 开发顺序

按以下顺序开发，每完成一项内部 demo 一次：

1. **第一步**：古典纪年工具（`timestamp.ts`），单元测试
2. **第二步**：StoryStorage 接口 + Vercel KV 实现
3. **第三步**：命主故事 LLM 生成（含 fallback）
4. **第四步**：故事页面 `/story/[storyId]`
5. **第五步**：命格卡二维码集成
6. **第六步**：PhraseStorage 接口 + 实现
7. **第七步**：金句批量生成
8. **第八步**：管理后台 `/admin/phrases`
9. **第九步**：业务层接入金句库
10. **第十步**：导出工具 + 文档

### 6.2 与作者的协作点

以下决策点必须等作者确认后再继续：

- **第三步**完成后：作者抽样审核 10 篇命主故事，确认文风正确
- **第七步**完成后：作者审核首批 30 句金句，确认调子正确
- **第十步**完成前：作者确认整体可用，再标记 v1.5 上线

### 6.3 不要做的事

- 不要为"看起来更专业"加注册系统
- 不要为"用户更方便"加自动登录
- 不要为"提高留存"加每日提醒
- 不要为"增加趣味"加排行榜
- 不要把临别金句生成功能直接交给玩家（必须经过审核）

如对以上任何一条有疑问，**先问作者再做**。

---

## 7. 文档与维护

### 7.1 在仓库中维护以下文档

```
/docs
  v1.5-design.md         本文档
  llm-prompts.md         所有 LLM prompt 的当前版本
  storage-schema.md      数据存储 schema
  migration-guide.md     跨平台迁移指南
  CHANGELOG.md           版本变更记录
```

### 7.2 prompt 版本管理

每次修改 LLM prompt：

1. 在 `llm-prompts.md` 增加新版本号（v1.5.0 → v1.5.1）
2. 旧版 prompt 保留在文档中（不删除，便于追溯）
3. 在生成的 record 中记录 `promptVersion`，便于回查哪些故事/金句是哪版 prompt 生成的

---

**文档版本**：v1.5.0
**作者**：程建都律师
**适用项目**：罗刹当铺（https://luocha-pawnshop.vercel.app）
**目标 Codex 实施周期**：5-10 个工作日（含审核迭代）
